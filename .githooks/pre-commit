#!/usr/bin/env bash
set -euo pipefail

make readme

if ! git diff --exit-code README.md >/dev/null 2>&1; then
  echo "README.md is out of date. Run 'make readme' and stage the result." >&2
  exit 1
fi
